{
  "name": "tourism-simulation-ui",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "evergreen-ui": "^4.21.5",
    "image-map-resizer": "^1.0.10",
    "react": "^16.11.0",
    "react-dom": "^16.11.0",
    "react-draggable": "^4.1.0",
    "react-router-dom": "^5.1.2",
    "react-scripts": "3.2.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "start:fake": "concurrently \"react-scripts start\" \"json-server --watch mocks/fake-api.json --port 5000 \"",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "concurrently": "^5.0.0",
    "json-server": "^0.15.1"
  }
}
